# Makefile for test. Automatically generated by SBV. Do not edit!

# include any user-defined .mk file in the current directory.
-include *.mk

CC=gcc
CCFLAGS?=-Wall -O3 -DNDEBUG -fomit-frame-pointer

all: test_driver

test.o: test.c test.h
	${CC} ${CCFLAGS} -c $< -o $@

test_driver.o: test_driver.c
	${CC} ${CCFLAGS} -c $< -o $@

test_driver: test.o test_driver.o
	${CC} ${CCFLAGS} $^ -o $@

clean:
	rm -f *.o

veryclean: clean
	rm -f test_driver
